name: Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v2
    - name: Build and test
      uses: docker://robot101/computational-workflows
      with:
        args: python -m pytest
